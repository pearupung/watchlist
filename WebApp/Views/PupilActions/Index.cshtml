@model IEnumerable<Domain.PupilAction>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ActionName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.HabitLoop)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ActionName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.HabitLoop.HabitLoopId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PupilActionId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PupilActionId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PupilActionId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
